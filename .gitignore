# Secrets — keep out at all costs
.env
.env.local
*.env

# Python
.venv/
__pycache__/
*.pyc

# Node / SvelteKit
node_modules/
.svelte-kit/
build/

# Local data
data/
*.db
*.db-journal

# macOS
.DS_Store
._*

# Editors
.vscode/
.idea/

# Claude Code local overrides
.claude/settings.local.json
CLAUDE.local.md

# Stale Caddy paths from earlier scaffold
caddy_data/
caddy_config/
access.log
analysis.md

# Local dev TLS overlay — matches https://focus.baltito.com locally.
# Lives only on the maintainer laptop; chemex pulls vanilla compose.
docker-compose.override.yml
Caddyfile.local

# Brand source files — exported variants live in focus/frontend/static/
brand/
